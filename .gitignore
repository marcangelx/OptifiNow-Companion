# Node
backend/node_modules/

# Terraform — state files can contain sensitive values (API keys, ARNs)
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars
terraform/*.tfvars.json

# Terraform plan files (binary, may contain sensitive data)
terraform/tfplan
terraform/*.tfplan

# Lambda build artifact (generated by terraform archive_file)
terraform/modules/lambda/lambda.zip

# OS
.DS_Store
Thumbs.db
